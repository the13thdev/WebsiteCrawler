{
  "name": "website_links_collector",
  "version": "1.0.0",
  "description": "Generates a list of urls of all web pages on a particular website (domain). ",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Vijit",
  "license": "ISC",
  "dependencies": {
    "cheerio": "^1.0.0-rc.1",
    "express": "^4.15.3",
    "morgan": "^1.8.2",
    "request": "^2.81.0"
  }
}
